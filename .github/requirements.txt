# Only for check_schema.py and its test; look.py needs nothing but Python.
# The two validators are what makes jsonschema check "date-time", "uri" and
# "uri-reference" at all: without them those formats pass unread.
jsonschema==4.26.0
rfc3339-validator==0.1.4
rfc3986-validator==0.1.1
